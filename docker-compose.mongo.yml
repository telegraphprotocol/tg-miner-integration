services:
  mongo:
    image: mongo:7
    container_name: telegraph-mongo
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - telegraph-mongo-data:/data/db

volumes:
  telegraph-mongo-data:
